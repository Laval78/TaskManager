# TaskManager

to fill the database with data, uncomment in Program.cs / чтоб заполнить базу данних раскоментируйте код начиная с 57-ой строки
code from line 57


//if (true)
//{
//    using var scope = app.Services.CreateScope();
//    var context = scope.ServiceProvider.GetRequiredService<TaskManagerContext>();
//    using var transaction = context.Database.BeginTransaction();

//    SeedDatabase.Clear(context);
//    await SeedDatabase.Seed(context);

//    transaction.Commit();
//}
